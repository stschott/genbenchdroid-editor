import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(theme => ({
    small: {
        height: '100px'
    },
    medium: {
        height: '200px'
    },
    large: {
        height: '300px'
    },
}));

export default useStyles;